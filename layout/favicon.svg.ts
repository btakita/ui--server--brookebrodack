import { heroicons_film_ } from '@btakita/ui--any--brookebrodack/icon'
const svg = '' + heroicons_film_<'server'>({
	// language=css
	css: `
svg { stroke: black; }
@media (prefers-color-scheme: dark) {
	svg { stroke: white; }
}
	`.trim()
})
export default svg
